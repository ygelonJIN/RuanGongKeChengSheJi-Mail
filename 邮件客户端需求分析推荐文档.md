# 邮件客户端软件需求分析 —— 90问推荐决策文档

> 软件工程课程设计 · 邮件客户端
> 生成时间：2026年5月9日

---

## 模块一：邮箱账号与认证（15问）

### 1.1 你主要使用哪个邮箱服务？

**推荐：全部支持（QQ邮箱、Gmail、企业邮箱、Outlook、163等）**

**理由：** 作为通用邮件客户端，覆盖主流协议（IMAP/SMTP）是基础能力。不同用户可能同时使用个人邮箱（Gmail/QQ/163）和工作邮箱（Outlook/企业邮箱），多账号支持是刚需。当前主流邮箱服务均基于标准 IMAP/SMTP 协议，技术实现路径一致：
- **QQ邮箱** → IMAP + 授权码
- **Gmail** → IMAP/SMTP + OAuth2
- **Outlook** → IMAP/SMTP + OAuth2
- **163/QQ企业** → IMAP/SMTP + 密码或授权码
- **企业邮箱（Exchange）** → IMAP 或 EWS

**实现成本：** 中等。主要工作量在于适配各平台的 OAuth2 流程和授权码引导页。

---

### 1.2 是否需要支持多个邮箱账号同时管理？

**推荐：需要（强制功能）**

**理由：** 绝大多数用户不会只使用一个邮箱。典型场景：
- 个人邮箱（QQ/Gmail） + 工作邮箱（企业/Outlook）
- 主邮箱 + 备用邮箱
- 不同项目/部门的不同企业邮箱

多账号管理是现代邮件客户端的标配功能（如 Thunderbird、Outlook）。需要做到：
- 账号独立认证、独立同步
- 统一收件箱聚合视图
- 账号间快速切换
- 每个账号独立的同步设置

**实现成本：** 低。IMAP 协议天然支持多账号，通过账号抽象层管理即可。

---

### 1.3 你使用的企业邮箱是什么类型？

**推荐：支持 Exchange (EWS) + IMAP 双模式**

**理由：** 企业邮箱类型多样，很难让用户预先判断类型。最佳方案是：
- **自动检测**：首次配置时尝试 IMAP 连接，失败后提示用户选择 Exchange
- **Exchange (EWS)**：支持日历、会议邀请（.ics）、联系人、全局地址列表（GAL）、邮件文件夹同步等功能，比 IMAP 更强大
- **IMAP**：兼容性最广，适合自建邮件服务器和部分企业邮箱

**推荐默认值：** 自动检测 IMAP；提供 EWS 高级选项入口。

---

### 1.4 是否需要支持企业单点登录（SSO）？

**推荐：当前阶段不需要，保留接口**

**理由：** SSO（OAuth 2.0 / SAML）适合大型企业环境。如果这是面向个人用户或中小企业的桌面客户端，实现 SSO 的开发成本与收益不成比例。

**建议方案：**
- 当前版本：支持 OAuth2 登录（Gmail/Outlook 已有，QQ邮箱用授权码）
- 未来扩展：预留 SAML/OIDC 接口，为企业版铺路
- 实际替代：Windows 用户可使用系统凭据管理器（Windows Credential Manager）简化登录

---

### 1.5 QQ邮箱需要授权码登录，是否了解这个流程？

**推荐：需要完整支持，且引导页要清晰**

**理由：** QQ邮箱的授权码机制（类似 Gmail 的 App Password）有其历史特殊性：
1. 用户需开启 IMAP 服务
2. 在 QQ邮箱设置 → 账户 → POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务 中生成授权码
3. 用授权码替代 QQ 密码登录

**实现要点：**
- 首次配置 QQ 邮箱时，弹出带截图的引导教程
- 明确告知授权码 ≠ QQ密码，保护用户安全意识
- 存储授权码而非真实密码

---

### 1.6 是否需要支持 OAuth2 第三方登录（Gmail、Outlook 推荐方式）？

**推荐：需要（强制功能）**

**理由：** OAuth2 是现代邮箱服务的标准认证方式，相比密码登录有显著优势：
- **安全性**：不存储密码，token 可随时撤销
- **用户体验**：避免"不够安全"的应用密码提示（Gmail 会拦截）
- **合规性**：Gmail 和 Outlook 的安全策略越来越严格，应用密码即将被逐步淘汰

**推荐实现：**
- Gmail → Google OAuth 2.0
- Outlook/Hotmail → Microsoft Identity Platform (MSAL)
- QQ/163 → 授权码（国区邮箱 OAuth2 暂不通用）

**实现成本：** 中等。Google 和 Microsoft 均提供成熟的 OAuth SDK。

---

### 1.7 密码和凭证如何存储？

**推荐：Windows 凭据管理器（Credential Manager）**

**理由：** 有三种常见方案对比：

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **Windows 凭据管理器** | 系统级加密、安全、与系统其他应用一致、支持 RDP 同步 | 仅限 Windows | ⭐⭐⭐ |
| 本地加密（DPAPI/AES） | 跨平台可控 | 需自行管理密钥，Windows 上仍依赖 DPAPI | ⭐⭐ |
| 配置文件明文 | 简单 | 极不安全 | ❌ |

**推荐方案详解：**
- Windows：使用 `Credentials` 或 `keyring` 库调用系统的凭据管理器
- 存储内容：OAuth token、IMAP 密码、授权码
- Token 刷新：自动处理 OAuth refresh token 续期
- 凭证隔离：每个账号独立存储，互不干扰

**补充说明：** Windows 凭据管理器可通过控制面板 → 凭据管理器查看，用户信任度高。

---

### 1.8 是否需要支持双因素认证（2FA）？

**推荐：不需要作为独立功能，但 OAuth2 本身隐含 2FA**

**理由：**
- 如果用户通过 OAuth2 登录 Gmail/Outlook，2FA 已在 Google/Microsoft 端完成
- 如果用户使用 IMAP+密码/授权码，2FA 是邮箱服务端的行为，客户端无需介入
- 客户端额外实现 2FA（如 TOTP）是面向"本地应用密码"场景，对本项目价值有限

**建议：** 在账号安全页面显示当前认证方式（Gmail账号已通过Google 2FA保护），增加用户信任感。

---

### 1.9 是否需要支持委托访问（如代收秘书邮件）？

**推荐：不需要，V1 版本排除**

**理由：** 委托访问（Delegation）是高级功能，技术实现复杂：
- Gmail 委托：需要 Gmail API 的 `Delegation` 权限
- Outlook 委托：需要 Exchange 的 `delegate` 权限
- UI 复杂度高：需要管理"代表谁发送"的切换

**优先级评估：** 用户面较窄，作为 V2 功能候选。

---

### 1.10 邮箱账号的连接超时时间你期望设置多长？

**推荐：默认 30 秒，支持用户自定义**

**理由：** 时间过短→弱网环境频繁失败；时间过长→卡顿体验差。

**分级超时策略：**
- **连接建立（TCP）**：15 秒
- **IMAP 登录**：30 秒
- **邮件同步（每次拉取）**：60 秒
- **SMTP 发送**：30 秒
- **OAuth 授权跳转**：5 分钟（浏览器弹窗）

**建议：** 在设置中提供"网络环境"快捷选项：快速网络（10s）/ 普通网络（30s）/ 慢速网络（60s），让用户一键适配。

---

### 1.11 是否需要支持别名邮箱的自动识别？

**推荐：不需要，V1 版本排除**

**理由：** 别名识别是一个语义理解功能：
- 需要解析邮箱地址的 `+` 别名（如 `user+shopping@domain.com`）
- 需要判断哪个是主账号、哪个是别名
- 大部分用户的别名用于过滤/追踪，不是核心痛点

**建议：** 可以通过搜索功能支持别名过滤（搜索 `user+*@domain.com`），而非主动识别。

---

### 1.12 IMAP 文件夹结构是否需要保持与邮箱服务器同步？

**推荐：需要（强制功能）**

**理由：** IMAP 的核心价值就是多端同步：
- 用户在手机客户端创建的文件夹，应出现在桌面客户端
- 用户在网页邮箱标记的已读状态，应同步到桌面
- 文件夹层级结构（包括中文命名）需要正确映射

**实现要点：**
- 首次同步时拉取完整文件夹列表（`LIST` 命令）
- 监听 `FLAGS`（已读/星标/删除）变更事件
- 订阅（SUBSCRIBE）重要文件夹
- 脱机模式下标记变更，上线后推送

---

### 1.13 是否需要支持邮件草稿的同步管理？

**推荐：需要（强制功能）**

**理由：** 草稿同步是 IMAP 的标准功能：
- 用户在桌面端写了一半的邮件，关机后手机继续写
- 草稿内容（Subject、Body、附件、元数据）完整同步
- 避免多端写同一封邮件产生冲突

**实现方案：**
- 写入时保存到本地 + 同步到 IMAP `[Gmail]/Drafts` 或 `Drafts`
- 退出时自动上传草稿
- 草稿列表页面显示所有账号的草稿

---

### 1.14 连接失败时需要自动重试吗？重试间隔和次数？

**推荐：需要（带指数退避策略）**

**理由：** 网络不稳定是常态，重试机制保证可靠性：

**推荐重试策略：**
- **首次重试**：2 秒后
- **第二次重试**：4 秒后（指数退避）
- **第三次重试**：10 秒后
- **第四次重试**：30 秒后
- **第五次重试**：60 秒后
- **最终失败**：提示用户，显示错误详情（超时 / 认证失败 / 网络不可达）
- **静默重试上限**：连续失败 3 次后改为手动重试（避免电量/流量浪费）

**特殊处理：**
- 用户主动下拉刷新 → 立即重试
- 后台同步 → 使用上述策略
- OAuth token 过期 → 触发重新授权流程

---

### 1.15 是否需要支持代理/VPN 环境下的邮箱连接？

**推荐：不需要单独适配，但需兼容系统代理设置**

**理由：** 代理/VPN 配置是系统级行为：
- Windows 系统代理设置 → 应用默认继承
- 使用 `winhttp` / 系统代理 API 自动读取
- SOCKS5 / HTTP 代理均通过系统层处理

**实现成本：** 极低（Python requests 库默认支持系统代理）。无需额外 UI。

---

## 模块二：桌面框架技术选型（10问）

### 2.1 你更看重安装包体积小还是开发速度？

**推荐：开发速度优先（Electron + React）**

**理由：** 在安装包体积与开发速度之间，需要权衡课程设计的时间约束：

| 框架 | 安装包 | 开发速度 | UI美观度 | 生态 | 推荐度 |
|------|--------|----------|----------|------|--------|
| **Electron + React** | 较大（~100MB+） | 快 | 高 | 极丰富 | ⭐⭐⭐ |
| Tauri + Web | 小（~10MB） | 中 | 高 | 一般 | ⭐⭐ |
| WPF/.NET | 小（~50MB） | 中 | 中 | Windows专有 | ⭐⭐ |
| Qt | 小（~30MB） | 慢 | 高 | 一般 | ⭐ |
| PyQt/PyGTK | 小 | 中 | 低 | 差 | ⭐ |

**推荐 Electron + React 的核心原因：**
1. **课程设计时间有限**：Electron + React 生态极为成熟，UI组件库（Ant Design、Material-UI）开箱即用，大幅缩短开发周期
2. **团队技能匹配**：前端技术（HTML/CSS/JS）是大多数计算机专业学生最熟悉的技能
3. **功能扩展容易**：系统托盘、右键菜单、全局快捷键、通知等均有现成 npm 包
4. **调试友好**：Chrome DevTools 支持，React DevTools 可视化调试
5. **需求满足**：Windows 独占目标，无需跨平台负担

**安装包优化：** 可使用 electron-builder 的 NSIS 安装程序，安装包控制在 80-150MB 属于可接受范围。

---

### 2.2 目标平台是 Windows 独占还是需要跨平台（Mac/Linux）？

**推荐：Windows 独占**

**理由：** 明确的需求约束：
- 课程设计项目交付物边界清晰
- Windows 用户基数大，测试环境易得
- Electron 天然支持 Windows，Windows API 调用方便
- 节省跨平台适配（macOS 菜单栏、Linux 系统通知）的额外工作量

---

### 2.3 是否需要系统托盘常驻运行？

**推荐：需要（关键功能）**

**理由：** 系统托盘是桌面邮件客户端的标配：
- **后台同步**：收最小化到托盘，后台保持邮件同步
- **快速操作**：右键菜单可快速写邮件、刷新、检查新邮件
- **通知指示**：托盘图标可显示未读数角标（Badge）
- **稳定运行**：防止用户误关窗口导致后台通知中断

**实现方案（Electron）：**
- 使用 `electron` 的 `Tray` API
- 左键：显示/隐藏主窗口
- 右键：显示上下文菜单（写邮件、刷新、检查所有账号、退出）
- 托盘图标动态更新（新邮件时改变图标颜色/显示角标）

---

### 2.4 是否需要原生通知功能？

**推荐：需要（强制功能）**

**理由：** 桌面通知是新邮件提醒的核心渠道：
- Windows 10/11 的 Action Center 通知
- 支持通知中心显示邮件摘要（发件人 + 主题 + 前50字预览）
- 点击通知直接跳转到对应邮件
- 支持通知内操作按钮："标记已读" / "快速回复"

**实现方案（Electron）：**
- 使用 Electron 的 `Notification` API（底层调用 Windows 通知）
- 每个账号独立通知开关
- 通知分组：多封新邮件合并为一条通知

---

### 2.5 是否需要全局快捷键支持？

**推荐：需要（基础快捷键）**

**理由：** 提升效率，减少鼠标依赖。推荐支持以下快捷键：

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+N` | 新建邮件 |
| `Ctrl+R` | 回复邮件 |
| `Ctrl+Shift+R` | 回复全部 |
| `Ctrl+F` | 转发邮件 |
| `Delete` | 删除邮件 |
| `Ctrl+Shift+Delete` | 彻底删除 |
| `Ctrl+U` | 标记未读 |
| `Ctrl+Shift+U` | 标记已读 |
| `Ctrl+D` | 归档 |
| `Ctrl+1/2/3` | 切换账号 |
| `Ctrl+Shift+I` | 收件箱 |
| `Ctrl+E` | 搜索框聚焦 |
| `Ctrl+,` | 设置 |
| `Escape` | 关闭弹窗/返回 |

**全局快捷键（在应用未聚焦时）：**
- `Ctrl+Alt+M`：打开应用窗口（可选，需用户开启）

**实现方案：** 使用 `electron` 的 `globalShortcut` 注册。

---

### 2.6 是否需要开机自启功能？

**推荐：不需要**

**理由：** 明确的需求排除：
- 用户主动说"不需要"
- 邮件客户端不是安全/通讯类必需应用
- 开机自启会增加用户电脑负担和启动时间
- 用户可自行通过任务管理器 → 启动项自行添加

**但建议：** 在设置中提供开关（用户可能自己改变主意）。

---

### 2.7 是否需要原生系统菜单栏集成？

**推荐：需要（Electron 默认支持）**

**理由：** Windows 原生菜单栏是应用的标准组成部分：
- **文件**：新建邮件、关闭账号、退出
- **编辑**：撤销、重做、剪切、复制、粘贴、全选
- **视图**：侧边栏开关、邮件预览开关、全屏、缩放
- **账户**：切换账号、同步所有账号
- **帮助**：关于、使用手册、检查更新
- **右键菜单**：邮件列表行右键（回复、删除、归档、标记）

**实现方案：** Electron `Menu` API 构建原生菜单，菜单内容与窗口功能联动。

---

### 2.8 是否需要文件拖拽到应用的功能？

**推荐：需要（中等推荐）**

**理由：** 附件是邮件的核心功能：
- **拖拽文件到窗口**：自动打开附件添加流程
- **拖拽邮件到文件夹**：移动邮件到指定文件夹
- **拖拽导出**：将邮件拖出窗口到文件夹（保存为 .eml 文件）

**实现方案（Electron）：**
- 使用 HTML5 拖放 API + Electron 的 `webContents` API
- 主窗口注册 `ondrop` 事件处理文件

---

### 2.9 是否需要与系统日历集成？

**推荐：不需要，V1 排除**

**理由：** 系统日历集成复杂度高：
- Windows：Outlook Calendar API 或 Google Calendar API
- 需要处理 .ics 会议邀请（RSVP 逻辑）
- UI 改动大：需增加日历视图

**替代方案：** 邮件中的日历邀请（.ics）可作为附件处理，解析后显示会议信息即可。

---

### 2.10 是否需要深色/浅色主题跟随系统？

**推荐：需要（三模式：亮/暗/跟随系统）**

**理由：** 现代桌面应用的标配体验：
- 程序员/夜间用户偏好深色模式
- 商务场景偏好浅色模式
- "跟随系统"是最省心的选择

**实现方案：**
- 使用 `prefers-color-scheme` CSS 媒体查询 + Electron 的 `nativeTheme` API
- 三个选项：浅色 / 深色 / 跟随系统
- 切换即时生效，无需重启应用

---

## 模块三：规则引擎设计（15问）

### 3.1 你期望的规则条件有哪些？

**推荐：支持以下全部条件类型**

| 条件类型 | 说明 | 优先级 |
|----------|------|--------|
| **主题关键词** | 包含/不包含/正则匹配 | ⭐⭐⭐ |
| **发件人** | 精确邮箱/域名/@后缀 | ⭐⭐⭐ |
| **收件人** | To/CC/BCC 任意包含 | ⭐⭐⭐ |
| **正文关键词** | 全文搜索/仅标题 | ⭐⭐⭐ |
| **附件** | 有/无/文件名包含/特定类型 | ⭐⭐ |
| **日期范围** | 早于/晚于/区间 | ⭐⭐ |
| **优先级标记** | 高优先级/普通/低优先级 | ⭐ |
| **收件时间** | 工作时间/非工作时间 | ⭐ |
| **邮件大小** | 大于/小于指定 KB/MB | ⭐ |
| **已读状态** | 已读/未读 | ⭐ |
| **星标状态** | 已标记星标/未标记 | ⭐ |
| **来源账号** | 指定账号 | ⭐⭐ |

**理由：** 条件越丰富，规则越灵活。IMAP 协议中这些字段均可通过 `SEARCH` 命令或本地过滤实现。

---

### 3.2 规则条件之间是 AND 还是 OR 逻辑组合？

**推荐：支持 AND + OR 混合逻辑**

**理由：** 现实场景复杂，单一逻辑不够用：
- **AND**：主题包含"周报" **且** 发件人是"boss@company.com"
- **OR**：发件人是"hr@company.com" **或** 发件人是"admin@company.com"

**实现方案：** 引入**条件组（Condition Group）**概念：
- 每个规则可包含多个条件组
- 组内条件默认 AND 关系
- 组间条件为 OR 关系
- 支持无限嵌套：`A AND (B OR C) AND (D OR E OR F)`

---

### 3.3 是否需要支持嵌套规则组？

**推荐：需要（最多支持2层嵌套）**

**理由：** 嵌套条件过于复杂会导致规则难以理解和维护：
- 1层嵌套（如推荐3.2所述）满足99%场景
- 2层嵌套（如 `A AND (B OR (C AND D)) OR E`）满足边缘场景
- 超过2层嵌套时，建议拆分为多条规则

**UI 设计：** 条件组用卡片/折叠面板展示，嵌套时缩进显示层级。

---

### 3.4 规则优先级如何定义？

**推荐：支持手动拖拽排序**

**理由：** 规则优先级直接影响处理结果：
- 如果规则1（移动到"工作"）先匹配，规则2（标记星标）可能永远不会触发
- 不同用户的优先级需求不同

**实现方案：**
- 规则列表支持鼠标拖拽排序
- 列表顶部 = 最高优先级
- 顶部显示"优先级越高，越先匹配"提示
- 冲突时按顺序逐一匹配，第一条匹配的规则执行

---

### 3.5 是否需要规则测试/预览功能？

**推荐：需要（强烈推荐）**

**理由：** 规则配置错误代价高，测试功能让用户安心：
- 选择一条规则 → 点击"测试"
- 显示过去30天内所有匹配该规则的邮件
- 高亮显示每个条件命中的部分
- 预览该规则将执行的操作

**价值：** 避免"误移动重要邮件到垃圾箱"的灾难性错误。

---

### 3.6 是否需要规则统计？

**推荐：需要（基础统计）**

**理由：** 统计帮助用户了解规则的实际效果：
- 每条规则执行了多少次
- 规则匹配最多的邮件（热力检测）
- 规则创建以来的累计处理量

**显示位置：** 规则列表页每条规则右侧显示计数器。

---

### 3.7 是否需要规则冲突检测？

**推荐：不需要，V1 排除**

**理由：** 冲突检测技术难度大：
- 需要判断多条规则对同一邮件的"影响力重叠"
- UI 警告可能造成用户困惑
- 用户可以通过手动排序解决优先级问题

**替代方案：** 通过"测试预览"功能让用户自己发现冲突。

---

### 3.8 当规则匹配时，需要执行哪些操作？

**推荐：支持以下全部操作**

| 操作 | 说明 | 优先级 |
|------|------|--------|
| **移动到文件夹** | 跨账号移动、本地文件夹 | ⭐⭐⭐ |
| **标记已读/未读** | 改变已读状态 | ⭐⭐⭐ |
| **标记星标/取消星标** | 加星标记重要 | ⭐⭐⭐ |
| **打标签** | 多标签支持（本地标签，非IMAP标签） | ⭐⭐ |
| **自动回复（Vacation）** | 度假自动回复 | ⭐⭐ |
| **转发到指定邮箱** | 静默转发（需SMTP） | ⭐⭐ |
| **删除（放入垃圾箱）** | 移入 [Gmail]/Trash 或 [Mail]/Deleted | ⭐⭐ |
| **彻底删除** | 永久删除（需确认） | ⭐ |
| **桌面通知** | 触发特殊通知（如老板邮件特殊提醒） | ⭐⭐ |
| **播放声音** | 自定义提示音 | ⭐ |
| **标记优先级** | 高/普通/低优先级 | ⭐ |
| **添加到日历** | 解析邮件中的日期创建日历事件 | ⭐ |

**理由：** 自动化是规则引擎的核心价值。操作越丰富，应用场景越广。

---

### 3.9 是否需要支持定时规则？

**推荐：不需要，V1 排除（技术可行但优先级低）**

**理由：** 定时规则需要：
- 可靠的定时任务调度器（后台进程常驻）
- 时区处理逻辑
- 与系统闹钟集成

**替代方案：** 通过"工作时间"条件过滤（见3.1日期范围），在邮件到达时判断是否在工作时间，仍可实现"工作时间才应用某规则"的需求。

---

### 3.10 是否需要默认规则？

**推荐：需要（至少一个兜底规则）**

**理由：** 默认规则防止邮件丢失：
- **兜底规则**：所有未匹配邮件 → 留在收件箱
- **垃圾箱规则**：主题/正文含"促销""折扣""点击这里"等 → 移动到垃圾箱
- **新闻订阅规则**：检测来自 mailing list → 移动到"订阅"文件夹

**实现：** 用户可禁用/删除默认规则，但首次启动时建议创建2-3条基础规则。

---

### 3.11 是否需要导入/导出规则功能？

**推荐：需要（推荐功能）**

**理由：** 实用性强的功能：
- **迁移**：重装系统后规则不丢失
- **分享**：用户可以分享自己精心设计的规则给朋友
- **备份**：规则也是一种资产

**格式建议：** JSON 格式（结构清晰、易于解析），兼容未来版本扩展。
```json
{
  "version": "1.0",
  "rules": [
    {
      "name": "周报识别",
      "conditions": [...],
      "actions": [...],
      "priority": 1,
      "enabled": true
    }
  ]
}
```

---

### 3.12 规则配置需要持久化存储吗？

**推荐：需要（强制功能）**

**理由：** 规则是用户数据，必须持久化：
- 存储位置：SQLite 数据库（与邮件数据库一致）
- 表结构：`rules(id, name, conditions_json, actions_json, priority, enabled, created_at, updated_at)`
- 自动保存：每次规则变更自动持久化（防丢失）
- 规则变更事件通过 Electron IPC 通知各渲染进程同步

---

### 3.13 是否需要支持预设规则模板？

**推荐：需要（强烈推荐）**

**理由：** 降低用户配置门槛：
- **周报识别**：主题含"周报"或"weekly report" → 标签[周报] + 移动到"周报"文件夹
- **HR重要通知**：发件人是 hr@company.com → 标记重要 + 桌面通知
- **退订处理**：正文含"取消订阅" → 移动到"订阅"文件夹 + 标记已读
- **发票识别**：附件含"发票"关键词 → 移动到"财务"文件夹
- **会议邀请**：含 .ics 附件 → 添加到日历 + 桌面通知

**UI：** 设置页面提供"从模板创建"入口，新用户可一键应用。

---

### 3.14 是否需要规则执行历史/日志？

**推荐：不需要，V1 排除**

**理由：** 日志功能增加存储压力和代码复杂度：
- 每封邮件的规则匹配记录都需要写日志
- 日志量随邮件数量线性增长
- 对于个人用户，统计（3.6）已经足够

**未来扩展：** 如果用户有合规/审计需求（如法律行业），可作为高级功能添加。

---

### 3.15 是否需要智能分类（AI自动判断邮件类别）？

**推荐：不需要，V1 排除**

**理由：** AI 分类（类似 Gmail 的"社交""促销""更新"分类）开发成本高：
- 需要本地 NLP 模型（体积大、运行慢）或云端 API（隐私问题、依赖外部服务）
- 分类准确性难以保证
- 规则引擎已经能覆盖大部分自动分类场景

**替代方案：** 通过预设模板（3.13）中的"周报识别""发票识别"等规则，实现半智能分类。

---

## 模块四：UI/UX设计（15问）

### 4.1 整体界面风格偏好？

**推荐：Outlook 风格（经典三栏布局）**

**理由：** 对比三种主流风格：

| 风格 | 特点 | 优点 | 缺点 | 推荐度 |
|------|------|------|------|--------|
| **Outlook 风格** | 三栏布局（导航+列表+预览） | 功能全面、符合 Windows 习惯 | 较复杂 | ⭐⭐⭐ |
| **Gmail 风格** | 两栏布局（紧凑列表+会话视图） | 简洁现代、移动端友好 | 功能密度低 | ⭐⭐ |
| **极简风格** | 单栏、极简元素 | 干净利落 | 功能受限 | ⭐ |

**推荐理由：** 邮件客户端功能复杂，Outlook 风格是经过20年用户习惯验证的布局方案：
- 左侧导航栏：账号、文件夹、标签一目了然
- 中间列表栏：邮件标题、时间、发件人、预览
- 右侧详情栏：邮件正文、附件、回复操作

**技术实现：** 使用 React + Ant Design 组件库，Ant Design 本身设计语言与 Outlook 风格接近。

---

### 4.2 是否需要文件夹树形导航？

**推荐：需要（左侧导航栏的核心组件）**

**理由：** 文件夹树是邮件管理的基础：
- 支持多级文件夹展开/折叠
- 显示各文件夹的未读数徽标（如 [收件箱 3]）
- 支持拖拽邮件到文件夹
- 支持文件夹右键操作（重命名、删除、设置为工作邮箱等）

**实现方案：** 使用 Ant Design 的 `Tree` 组件，支持虚拟滚动（文件夹多时）。

---

### 4.3 邮件列表是单栏还是双栏布局？

**推荐：双栏（列表+预览），支持切换**

**理由：** 两种布局各有优势：

- **单栏（仅列表）**：邮件列表窄，显示更多邮件标题，适合快速浏览
- **双栏（列表+预览）**：选中邮件后右侧显示预览，无需打开新窗口，适合深度阅读
- **三栏（列表+预览+详情）**：点击预览中的链接/附件时才展开详情

**推荐方案：** 采用 Outlook 的三栏布局（实际是四栏概念）：
1. 左侧：文件夹树（可折叠）
2. 中间：邮件列表
3. 右侧：邮件预览/详情面板（可隐藏）

**理由：** 桌面端屏幕空间充足，三栏布局效率最高。用户可通过 View 菜单隐藏右侧预览窗格。

---

### 4.4 邮件详情查看需要富文本渲染还是纯文本优先？

**推荐：富文本渲染（支持切换）**

**理由：** 现代邮件普遍使用 HTML 富文本：
- 支持图片内嵌、彩色样式、链接
- 纯文本模式会让邮件显得"破碎"
- 但某些场景（纯文本邮件、技术邮件）纯文本更清晰

**实现方案：**
- 默认使用 `DOMPurify` + 自定义 CSS 渲染 HTML 邮件
- 提供"纯文本视图"切换按钮
- 安全处理：过滤 `<script>`、`onclick` 等危险标签（XSS防护）
- 支持显示邮件原始源码（技术用户需求）

---

### 4.5 是否需要支持邮件会话/对话视图？

**推荐：不需要，V1 排除**

**理由：** 对话视图（Thread View）复杂度高：
- 需要根据 `In-Reply-To` 和 `References` 头字段重组邮件关系
- 多人多回复链条的展示逻辑复杂
- Gmail 的对话视图至今仍有争议（有人喜欢有人讨厌）
- 列表视图对邮件管理更直接

**替代方案：** 列表视图 + 邮件详情页的"查看对话"按钮（显示相关邮件列表），满足偶尔需求。

---

### 4.6 搜索功能需要多强大？

**推荐：基础关键词 + 搜索语法 + 高级筛选器**

**理由：** 搜索是邮件客户端的核心功能，需求分层：

**基础搜索：**
- 全局搜索框（Ctrl+E）
- 支持当前文件夹内搜索
- 实时搜索建议（基于历史搜索词）

**搜索语法（高级用户）：**
- `from:zhangsan` — 搜索发件人
- `subject:周报` — 搜索主题
- `has:attachment` — 有附件
- `is:unread` — 未读邮件
- `after:2026/1/1 before:2026/5/1` — 日期范围
- `larger:5M` — 大于5MB
- `from:@company.com` — 某公司域名

**高级筛选器（UI操作）：**
- 通过下拉菜单选择字段 + 条件 + 值
- 生成搜索语法或直接作为规则条件

**实现方案：** IMAP SEARCH 命令 + 本地 SQLite FTS5 全文索引（见5.3）。

---

### 4.7 搜索结果需要实时高亮匹配词吗？

**推荐：需要（仅高亮主题和发件人，不高亮正文）**

**理由：** 适度高亮提升体验：
- 搜索结果列表中，高亮匹配关键词（红色/黄色背景）
- 邮件详情页中，高亮正文中的匹配词（但正文可能很长，全文高亮反而干扰阅读）

**实现方案：**
- 列表页高亮：用 `<mark>` 标签包裹匹配词
- 正文高亮：提供"在正文中查找"功能，Ctrl+F 风格，逐个高亮

---

### 4.8 是否需要支持键盘快捷键操作？

**推荐：需要（已在2.5中详细定义）**

**理由：** 键盘操作是高效邮件处理的必备功能，详见2.5节表格。

**补充：** 在设置页面提供快捷键速查表。

---

### 4.9 拖拽操作习惯于哪种？

**推荐：拖拽到文件夹标签**

**理由：** 这是最直觉的操作方式：
- 选中一封或多封邮件
- 拖动到左侧文件夹树的目标文件夹上
- 松开后邮件被移动，文件夹树更新未读数

**实现要点：**
- 拖拽时显示半透明邮件预览
- 目标文件夹高亮显示（指示可放置区域）
- 不可放置的文件夹（如只读文件夹）显示禁止图标
- 支持跨账号拖拽移动

---

### 4.10 多选邮件的交互方式？

**推荐：Checkbox 多选 + Shift/Ctrl 范围选，两者都需要**

**理由：** 不同场景需要不同选择方式：

| 场景 | 推荐方式 | 实现 |
|------|----------|------|
| 选择特定几封不连续的邮件 | Ctrl+点击多选 | ⭐⭐⭐ |
| 选择连续范围的邮件 | Shift+点击首尾 | ⭐⭐⭐ |
| 全选当前列表 | Ctrl+A | ⭐⭐⭐ |
| 批量操作工具栏 | Checkbox 列 | ⭐⭐⭐ |

**实现方案：**
- 每行邮件左侧显示 Checkbox
- 选中邮件后，顶部出现批量操作工具栏（删除、标记已读、移动、归档）
- 选中数量显示在工具栏上（"已选择 5 封"）

---

### 4.11 是否需要邮件预览窗格可调整宽度/隐藏？

**推荐：需要（强制功能）**

**理由：** 用户工作场景多样：
- 大屏幕用户：希望预览窗格宽一些
- 小屏幕用户：希望隐藏预览窗格，只用列表
- 写邮件时：希望预览窗格完全隐藏

**实现方案：**
- 预览窗格与列表之间用可拖拽分隔线
- 鼠标拖拽调整宽度（最小宽度限制防止过窄）
- 双击分隔线：切换预览窗格显示/隐藏
- View 菜单提供快捷切换

---

### 4.12 标签和文件夹如何区分和管理？

**推荐：文件夹为主、标签为辅，视觉上明显区分**

**理由：** IMAP 原生只有文件夹，标签是扩展概念：

**文件夹：**
- 树形结构展示（左侧导航）
- 严格层级关系
- 邮件只能在一个文件夹中（移动操作）

**标签（本地扩展）：**
- 扁平列表展示（在文件夹树下方）
- 彩色标签（每标签一种颜色）
- 邮件可同时有多个标签（类似 Gmail）
- 存储在本地 SQLite，不同步到 IMAP 服务器

**UI 设计：**
- 文件夹：文件夹图标 + 层级缩进
- 标签：彩色圆点 + 扁平横排（类似 Slack 标签）
- 右键菜单：文件夹→新建/重命名/删除；标签→新建/编辑颜色/删除

---

### 4.13 是否需要支持邮件拖拽排序？

**推荐：不需要，V1 排除**

**理由：** 邮件的逻辑顺序由时间决定：
- 拖拽排序适合任务/TODO列表（人工优先级）
- 邮件列表按时间降序是标准约定
- 排序维度（时间、发件人、主题）通过表头点击切换即可

**替代方案：** 提供多种排序选项（见4.14），满足不同浏览需求。

---

### 4.14 列表排序需要支持哪些维度？

**推荐：以下全部维度**

| 排序维度 | 默认 | 说明 |
|----------|------|------|
| **时间（降序）** | ✅ 默认 | 最新的邮件在最上 |
| 时间（升序） | | 最早的邮件在最上 |
| 发件人（A-Z / Z-A） | | 按发件人名字母序 |
| 主题（A-Z / Z-A） | | 按主题字母序 |
| 优先级（高→低） | | IMAP 的 `\Flagged` 优先级 |
| 标记状态 | | 星标邮件置顶 |
| 未读状态 | | 未读邮件置顶 |
| 附件状态 | | 有附件的邮件置顶 |

**多级排序：** 支持主排序 + 次排序（如：按发件人排序，相同时按时间排序）。

---

### 4.15 统计数据展示需要哪些图表类型？

**推荐：以下图表类型**

| 图表类型 | 用途 | 库推荐 |
|----------|------|--------|
| **折线图** | 每日/周/月收件趋势 | Recharts |
| **柱状图** | 各文件夹邮件数量对比 | Recharts |
| **饼图/环形图** | 邮件类型分类占比（工作/社交/营销） | Recharts |
| **日历热力图** | 每日收件量热力图（类似 GitHub 贡献图） | react-calendar-heatmap |
| **Top 10 发件人** | 水平柱状图 | Recharts |
| **响应时间散点图** | 发出邮件→收到回复的响应时间分布 | Recharts |

**理由：** 统计图表是数据分析模块的核心视觉组件，Recharts 是 React 生态中最成熟的图表库，轻量、声明式、TypeScript 支持良好。

---

## 模块五：数据存储与隐私（15问）

### 5.1 邮件内容是否本地缓存？

**推荐：全部缓存（标题+正文）**

**理由：** 本地缓存是离线体验的基础：
- 无网时仍可阅读历史邮件
- 加快邮件列表加载速度
- 支持全文搜索（离线可用）

**缓存策略（分层）：**
1. **邮件标题缓存**：首次同步时缓存（发件人、主题、时间、摘要），始终完整
2. **邮件正文缓存**：按需下载，首次查看时缓存到本地
3. **附件元数据**：标题、大小、类型缓存；文件本体按需下载
4. **附件缓存**：打开时下载，标记"已下载"后可离线访问

**空间管理：** 附件本地缓存最大占用可通过设置限制（见5.4）。

---

### 5.2 缓存存储用什么格式？

**推荐：SQLite（单文件数据库）**

**理由：** 对比主流存储方案：

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **SQLite** | 成熟稳定、查询高效、支持全文索引、单文件、跨平台 | 单文件有大小上限（TB级无压力） | ⭐⭐⭐ |
| IndexedDB | 浏览器原生、无需安装 | 浏览器限制、查询能力弱 | ⭐ |
| 文件系统（EML文件） | 直观、易于导出 | 查询极慢、难以管理关系 | ⭐ |
| LevelDB/RocksDB | 高性能、LSM树 | 生态不如 SQLite | ⭐⭐ |

**推荐理由：** SQLite 是桌面应用本地存储的最佳选择：
- 单数据库文件：`mailclient.db`
- 关系型数据（账号、规则、邮件元数据）用表结构
- 全文搜索用 FTS5 虚拟表
- 附件存储在 `attachments/` 文件夹，通过 BLOB 引用或文件路径
- 备份简单：复制单个 `.db` 文件即可

---

### 5.3 全文搜索需要本地索引吗？

**推荐：需要（SQLite FTS5）**

**理由：** IMAP 服务器搜索能力有限且慢，本地索引是必然选择：
- **速度**：本地 FTS5 搜索 < 100ms，IMAP SEARCH 可能需要数秒
- **功能**：支持中文分词、模糊搜索、布尔查询
- **离线**：索引在本地，网络断开仍可搜索
- **安全**：邮件内容不经过第三方服务器

**技术方案：**
- 使用 SQLite FTS5（全文搜索虚拟表引擎）
- 中文分词：使用 `jieba` 分词库（Python）/ `nodejieba`（Node.js）
- 索引内容：邮件主题、发件人、正文（前512字）、附件名
- 索引更新：邮件下载时增量更新，删除时增量删除

**搜索结果排序：** FTS5 内置 BM25 算法排序，匹配度高的结果优先。

---

### 5.4 本地存储最大允许占用多大空间？

**推荐：默认 5GB，可用户配置（1GB - 50GB）**

**理由：** 平衡存储空间与用户体验：
- 5GB 默认可缓存数万封邮件（含正文，不含附件）
- 附件单独计算（用户可能收到大文件）
- 超过限制时，按 LRU（最近最少使用）策略清理旧缓存

**存储占用显示：** 在设置页面显示当前使用量：
```
邮件缓存：2.3 GB / 5 GB
附件缓存：1.1 GB / 10 GB
数据库：45 MB
总计：3.5 GB
```

---

### 5.5 是否需要数据加密存储？

**推荐：不需要，V1 排除**

**理由：** 加密集存储开发成本高，且本应用场景中收益有限：
- 邮件已通过 IMAPS（TLS 加密传输）保护
- 邮件内容对邮箱服务器是可见的，本地加密意义有限
- 用户如果需要高安全，可使用 BitLocker/VeraCrypt 全盘加密

**替代方案：** 使用 Windows DPAPI 对敏感字段（如 OAuth token）进行单字段加密存储。数据库本身不整体加密。

---

### 5.6 是否需要端到端加密（邮件内容即使你服务端也不可见）？

**推荐：不需要，V1 排除**

**理由：** E2EE 是一个独立且复杂的功能：
- 需要 PGP/SMIME 密钥管理
- 密钥交换和验证的 UX 极其复杂
- 违反"接收他人发来的未加密邮件"的需求
- Gmail/Outlook 等主流邮件服务均不提供 E2EE

**适用场景：** 极少数安全敏感用户，属于未来扩展方向。

---

### 5.7 是否需要自动清理旧数据功能？

**推荐：需要（按保留期限自动清理）**

**理由：** 防止存储无限增长：
- **自动清理规则**：超过 N 天的邮件正文，保留标题但删除本地正文缓存
- **附件清理**：超过 N 天且未打开的附件，自动删除本地副本（保留服务器副本）
- **垃圾箱自动清空**：超过 30 天的邮件自动彻底删除

**用户可配置：**
- 保留策略：保留所有 / 仅保留 N 天 / 仅保留 N 个月的邮件正文
- 清理触发时机：应用启动时 / 手动触发 / 定时每日凌晨

---

### 5.8 用户数据存储路径需要可配置吗？

**推荐：不需要，V1 使用默认路径**

**理由：** 简化配置，降低用户困惑：
- **Windows 默认路径：** `%APPDATA%/MailClient/`（如 `C:\Users\用户名\AppData\Roaming\MailClient\`）
- 该路径已受 Windows 用户账户控制（UAC）保护
- 大多数用户不需要更改存储位置

**建议：** 在"关于"页面显示当前存储路径（用户可自行复制到资源管理器打开），但不做配置入口。

---

### 5.9 是否需要数据导出功能？

**推荐：需要（支持 EML 格式）**

**理由：** 数据导出是用户数据所有权的体现：
- **EML 格式**：国际标准邮件格式，单封邮件一个 .eml 文件（RFC 822）
- **JSON 格式**：规则、设置、账号配置的导出
- **MBOX 格式**：批量导出到标准邮箱归档格式（进阶功能）

**导出选项：**
- 选中邮件 → 导出为 EML
- 文件夹导出 → 导出为 MBOX 或 ZIP(EML)
- 全部数据 → 导出 ZIP 包（EML + 附件 + 数据库备份）

---

### 5.10 是否需要数据导入功能？

**推荐：需要（支持 EML/MBOX 导入）**

**理由：** 迁移场景的硬需求：
- **从其他邮件客户端迁移**（Thunderbird、Outlook、Foxmail）
- **导入历史邮件备份**（EML 文件）
- **MBOX 导入**：Thunderbird/Outlook 导出格式

**实现方案：**
- EML 导入：解析 MIME 格式，写入本地数据库
- MBOX 导入：逐条解析 .mbox 文件（RFC 4155）
- 导入进度显示 + 冲突处理（重复邮件判断）

---

### 5.11 是否需要自动备份本地数据库？

**推荐：不需要，V1 排除**

**理由：** 邮件数据的安全性由用户自行负责：
- 邮件服务器（IMAP）是主要数据源，客户端缓存是辅助
- 自动备份增加代码复杂度
- 用户可通过 Windows 任务计划程序自行设置备份

**替代方案：** 提供手动备份入口（设置页 → 备份 → 一键导出 ZIP）。

---

### 5.12 会话 token 如何刷新和管理？

**推荐：自动透明刷新 + 手动重授权**

**理由：** OAuth token 有有效期，需要合理管理：

| Token 类型 | 有效期 | 刷新策略 |
|------------|--------|----------|
| Google OAuth Access Token | 1小时 | 自动用 Refresh Token 刷新 |
| Google OAuth Refresh Token | 长期（手动撤销） | 持久化存储，失效时提示重新授权 |
| IMAP 密码/授权码 | 永久（直到用户修改） | 不需要刷新 |
| Session Cookie（Webmail） | 视服务器而定 | 通过 IMAP 持久连接处理 |

**实现方案：**
- Access Token 过期时，自动使用 Refresh Token 获取新 Token
- Refresh Token 也失效时（如用户撤回了授权），弹出重新授权界面
- Token 存储在 Windows 凭据管理器（见1.7）

---

### 5.13 是否需要隐私模式（隐藏邮件内容）？

**推荐：不需要，V1 排除**

**理由：** 隐私模式增加 UI 复杂度，且替代方案充足：
- Windows 有多个用户账户系统
- 离开时按 `Win+L` 锁定屏幕即可
- Electron 支持 `BrowserWindow.setAlwaysOnTop()` 但不隐藏内容

**替代方案：** 提供"锁定应用"功能（需要 PIN 码才能再次访问），替代隐私模式。

---

### 5.14 邮件中的图片加载策略？

**推荐：默认不加载外部图片，显示占位符**

**理由：** 图片加载有安全和隐私风险：
- **追踪像素**：邮件中的 1x1 透明图片可追踪你是否打开了邮件（已读回执的替代方案）
- **隐私泄露**：加载图片会向第三方服务器暴露你的 IP 地址
- **流量消耗**：大邮件含多张图片时加载慢

**UI 设计：**
- 邮件正文中的外部图片显示为灰色占位符 + "图片被屏蔽，点击加载"按钮
- 附件中的图片直接显示（本地文件，无隐私风险）
- 设置中提供选项："始终加载图片" / "仅在发件人白名单中加载" / "从不加载"

---

### 5.15 是否需要追踪像素拦截功能？

**推荐：不需要，作为5.14的子功能实现**

**理由：** 追踪像素拦截是图片加载策略的自然延伸：
- 外部图片默认不加载 → 追踪像素天然被拦截
- 不需要单独设计"追踪像素拦截"功能
- 5.14 的默认不加载策略已覆盖此需求

---

## 模块六：统计与分析（10问）

### 6.1 需要统计哪些基础指标？

**推荐：以下全部基础指标**

| 指标 | 说明 | 计算方式 |
|------|------|----------|
| **每日/周/月收件数量** | 时间维度的收件趋势 | 按日期聚合 COUNT |
| **未读数** | 当前各文件夹/账号未读邮件数 | FLAG \Seen = 0 |
| **已处理数** | 已读 + 已归档 + 已删除的邮件数 | 状态字段统计 |
| **发送数量** | 统计已发送邮件 | SMTP 发送日志 |
| **平均每日收件量** | 长期趋势参考 | 总收件数 / 天数 |
| **峰值时段** | 收件量最高的时段 | 按小时聚合 |

**实现：** SQLite 查询 + Redis 风格的时序聚合（在 SQLite 中通过 `GROUP BY date` 实现）。

---

### 6.2 是否需要发件人分布统计？

**推荐：需要（Top N 发件人排行榜）**

**理由：** 发件人统计揭示邮件社交图谱：
- 谁最常联系你？
- 哪些合作伙伴邮件量最大？
- 垃圾邮件来源统计（辅助判断过滤规则效果）

**UI 设计：** 环形图 + Top 10 列表，支持按时间段筛选。

---

### 6.3 是否需要邮件类型分类统计？

**推荐：需要（基于规则的半自动分类）**

**理由：** 邮件分类统计帮助用户了解收件构成：
- **工作邮件**：来自公司域名
- **个人邮件**：来自个人邮箱服务商
- **营销邮件**：来自已知的营销邮件发送平台
- **社交邮件**：来自社交媒体通知（LinkedIn、微信等）
- **财务邮件**：来自银行、券商、发票相关

**实现方式：** 通过预设规则模板（见3.13）自动打标签，统计各标签邮件数量。无需 AI 判断。

---

### 6.4 是否需要响应时间统计？

**推荐：不需要，V1 排除**

**理由：** 响应时间统计复杂度高且实用性有限：
- 需要匹配"发出邮件"与"收到回复"的关联关系（Subject 匹配、时间窗口）
- 用户主动发起沟通的场景不一定是"需要回复"的工作场景
- 统计结果容易产生误导（收到自动回复算不算响应？）

**替代方案：** 可在邮件详情页显示"此邮件的回复情况"（如果有），但不做全局统计。

---

### 6.5 是否需要 Inbox Zero 达成天数统计？

**推荐：不需要，V1 排除**

**理由：** 统计价值有限：
- Inbox Zero 是个人工作习惯，与软件功能无关
- 达成天数不反映工作效率（工作性质决定邮件量）
- 开发者实现成本与收益不成比例

**替代方案：** 统计"收件箱清理效率"（收件数 vs 处理数的比率），更有参考价值。

---

### 6.6 是否需要规则执行效果统计？

**推荐：需要（已在3.6中定义）**

**理由：** 规则执行统计已在3.6节推荐，统计每条规则处理了多少封邮件，帮助用户评估规则有效性。

---

### 6.7 统计数据需要可视化图表吗？

**推荐：需要（已与4.15合并规划）**

**理由：** 统计数据 + 可视化图表是现代邮件客户端的标配：
- 纯数字统计不够直观
- 图表帮助发现趋势和异常
- 仪表盘展示提升用户成就感

**图表类型详见4.15节。**

---

### 6.8 是否需要邮件趋势分析？

**推荐：需要（工作日 vs 周末、高峰时段）**

**理由：** 趋势分析揭示工作模式：
- **周度趋势**：周一到周五 vs 周末的收件量对比
- **日内趋势**：24小时分布，找出邮件高峰时段
- **月度趋势**：跨月对比，看是否有周期性规律

**UI 设计：**
- 折线图：X轴为日期/Y轴为收件量
- 柱状图：X轴为小时/Y轴为平均收件量（按工作日/周末分组）

---

### 6.9 统计数据需要定期推送/报告功能吗？

**推荐：不需要，V1 排除**

**理由：** 定期报告增加不必要复杂性：
- 需要定时任务调度
- 需要邮件发送功能（报告推送）
- 大多数用户不会定期查看报告

**替代方案：** 用户随时打开统计页面查看最新数据即可。

---

### 6.10 是否需要自定义仪表盘？

**推荐：不需要，V1 使用固定仪表盘**

**理由：** 自定义仪表盘增加 UI 复杂度：
- 需要拖拽布局、显示/隐藏卡片、保存布局
- 开发工作量大
- 固定仪表盘（折线图+柱状图+饼图+Top10）已覆盖核心需求

**未来扩展：** V2 时提供仪表盘卡片开关设置（简单版自定义）。

---

## 模块七：通知系统（10问）

### 7.1 新邮件通知需要即时推送还是轮询？

**推荐：IMAP IDLE（推送）+ 定时轮询（兜底）**

**理由：** IMAP IDLE 是最理想的方案：

| 方案 | 原理 | 延迟 | 资源消耗 | 推荐度 |
|------|------|------|----------|--------|
| **IMAP IDLE** | 服务器推送变更 | < 1秒 | 低（TCP 长连接） | ⭐⭐⭐ |
| 定时轮询（1分钟） | 客户端定期拉取 | 1分钟 | 中 | ⭐⭐ |
| 定时轮询（5分钟） | 客户端定期拉取 | 5分钟 | 低 | ⭐ |
| 定时轮询（15分钟+） | 客户端定期拉取 | 很长 | 低 | ⭐ |

**推荐理由：**
- IMAP IDLE（RFC 2177）：在 IMAP 连接上发送 `IDLE` 命令，服务器有新邮件时立即推送通知，无需客户端频繁拉取
- 定时轮询作为兜底：每5分钟检查一次（应对 IDLE 连接断开的情况）
- IDLE 断连自动重连机制

**实现成本：** 中等。大多数 IMAP 库（Python imaplib / nodemailer / MailCore2）均支持 IDLE。

---

### 7.2 通知需要邮件预览内容吗？

**推荐：需要（显示前100字预览）**

**理由：** 通知预览让用户决定是否立即查看：
- 发件人 + 主题 + 前100字正文
- 用户可在不打开应用的情况下判断邮件重要性
- 决定是否中断当前工作立即处理

**安全提醒：** 预览文字中如含敏感信息（如验证码），存在一定隐私风险，可在设置中关闭预览。

**实现：** Windows 通知支持显示多行文本，Electron `Notification` API 直接支持。

---

### 7.3 是否需要按规则控制通知？

**推荐：需要（基于规则的通知控制）**

**理由：** 不是所有邮件都需要弹通知：
- 来自老板的新邮件 → 声音+弹窗+角标
- 来自新闻订阅的邮件 → 仅角标
- 来自垃圾箱/归档的邮件 → 完全不通知
- 会议邀请 → 特殊声音+特殊角标

**实现方案：**
- 每条规则可设置"通知行为"（见3.8操作列表）
- 在通知规则配置页面提供快速开关：
  - 文件夹级别：收件箱 / 草稿 / 已发送 / 自定义文件夹
  - 账号级别：每个账号的全局通知开关
  - 发件人白名单：来自白名单的邮件永远通知

---

### 7.4 通知的声音和震动需要可配置吗？

**推荐：需要（声音可配置，震动不适用桌面）**

**理由：** 桌面端声音通知的重要性：
- **无声办公环境**：需要关闭声音或使用耳机提示
- **个性化**：用户可能希望不同类型的邮件用不同声音
- **声音开关**：全局静音按钮（一键静音）

**实现方案：**
- 提供 3-5 个内置提示音选项（默认 / 简洁 / 重要 / 无声）
- 支持自定义提示音文件（.mp3/.wav）
- 任务栏托盘右键菜单提供快速静音按钮
- Windows 的震动（N/A）不适用，忽略

---

### 7.5 是否需要勿扰模式？

**推荐：需要（定时勿扰 + 手动勿扰）**

**理由：** 现代工作节奏需要专注：
- **定时勿扰**：每天 22:00 - 次日 8:00 自动静音
- **手动勿扰**：一键开启/关闭（托盘菜单快捷操作）
- **勿扰时策略**：仅收集通知，不弹窗、不出声
- **勿扰结束后**：显示勿扰期间收到的新邮件摘要

**实现方案：**
- 设置页配置勿扰时间窗口
- 托盘图标显示勿扰状态（灰色图标表示勿扰中）
- 勿扰期间邮件正常同步，只是延迟通知

---

### 7.6 是否需要专注模式？

**推荐：不需要，V1 排除**

**理由：** 专注模式（仅通知指定发件人）与勿扰模式有重叠：
- 通过通知规则（7.3）的白名单机制可以实现类似效果
- 专注模式是勿扰模式的变体，本质是"反向勿扰"
- 增加单独功能会增加 UI 复杂度

**替代方案：** 在勿扰模式中增加"仅通知以下发件人"的配置项，实现专注模式功能。

---

### 7.7 是否需要桌面角标/应用图标未读数显示？

**推荐：需要（托盘角标 + 任务栏角标）**

**理由：** 未读数角标是最直觉的通知指示：
- **任务栏图标角标**：Windows 任务栏显示未读总数（如 Thunderbird、Outlook）
- **托盘图标角标**：系统托盘图标覆盖层显示数字
- **托盘 Tooltip**：鼠标悬停显示"X 封未读邮件"

**实现方案（Electron）：**
- 任务栏：`BrowserWindow.setOverlayIcon()` （Windows 8+ 支持）
- 托盘：`Tray.setToolTip()` 显示文本信息

---

### 7.8 Windows 通知需要支持 Action 按钮吗？

**推荐：需要（基础 Action 按钮）**

**理由：** Action 按钮让通知变成快捷操作入口：
- Windows 10/11 通知支持 Action 按钮（最多3个）
- 常见按钮：
  - **"标记已读"**：不打开应用，直接标记
  - **"删除"**：快速删除可疑邮件
  - **"查看"**：打开应用并跳转到该邮件

**实现方案：**
- Electron `Notification` API 的 `actions` 选项
- 通过 IPC 通知主进程执行对应操作

**注意：** Action 按钮在 Windows 上的实现有兼容性限制，需测试。

---

### 7.9 通知送达后是否需要点击跳转到对应邮件？

**推荐：需要（强制功能）**

**理由：** 这是通知的基本价值：
- 点击通知 → 应用窗口前置 → 滚动到对应邮件 → 自动选中
- 需要从托盘恢复窗口时，点击托盘图标即可

**实现方案：**
- 通知点击事件 → `BrowserWindow.show()` + `BrowserWindow.focus()`
- IPC 消息传递邮件 ID → 渲染进程滚动到对应行 + 选中

---

### 7.10 是否需要通知历史记录查看？

**推荐：不需要，V1 排除**

**理由：** 通知历史功能必要性低：
- 通知本质是"即时提醒"，历史价值有限
- 邮件本身在收件箱中，永久可查
- Windows Action Center 已有通知历史（系统级），无需应用内重复实现
- 开发成本（存储历史通知、分页展示）与收益不匹配

---

## 总结：推荐功能一览

### 强制功能（V1 必须实现）

| 模块 | 功能 |
|------|------|
| 邮箱认证 | 全邮箱服务支持、多账号管理、OAuth2、授权码、凭据管理器 |
| 桌面框架 | Electron+React、系统托盘、原生通知、快捷键、跟随系统主题 |
| 规则引擎 | 多种条件、多操作、AND/OR逻辑、手动排序、测试预览、持久化 |
| UI设计 | Outlook三栏布局、文件夹树、富文本渲染、搜索语法、批量操作 |
| 数据存储 | SQLite全文索引、分层缓存、自动清理、EML导入导出 |
| 统计 | 基础指标、发件人统计、图表可视化、趋势分析 |
| 通知 | IMAP IDLE推送、预览内容、规则通知、勿扰模式、角标显示、跳转 |

### 推荐但不强制（V1 实现）

| 模块 | 功能 |
|------|------|
| 邮箱认证 | 自动检测企业邮箱类型、分级超时配置 |
| 桌面框架 | 文件拖拽、代理兼容 |
| 规则引擎 | 导入/导出、预设模板、统计 |
| UI设计 | 单栏/双栏切换、纯文本视图、搜索高亮、多级排序 |
| 数据存储 | 用户可配置存储路径 |
| 通知 | Action按钮 |

### V1 排除功能（未来扩展）

| 模块 | 功能 | 原因 |
|------|------|------|
| 邮箱认证 | SSO、委托访问、别名识别、2FA | 用户面窄或技术复杂 |
| 桌面框架 | 开机自启、系统日历集成 | 用户明确排除 |
| 规则引擎 | 嵌套规则组、定时规则、AI分类、规则日志 | 复杂度高或价值有限 |
| UI设计 | 对话视图、邮件拖拽排序、自定义仪表盘 | 争议大或开发成本高 |
| 数据存储 | 全量加密、E2EE、自动备份 | 安全/备份已有替代方案 |
| 统计 | 响应时间、Inbox Zero天数、定期推送报告 | 实用性有限 |
| 通知 | 专注模式、通知历史 | 与现有功能重叠 |

---

## 技术架构建议

### 推荐技术栈

| 层级 | 技术选型 | 理由 |
|------|----------|------|
| 桌面框架 | Electron 28+ | 成熟稳定，前端技术栈，生态丰富 |
| 前端框架 | React 18 + TypeScript | 组件化、类型安全、生态成熟 |
| UI组件库 | Ant Design 5 | 企业级设计语言，与Outlook风格契合 |
| 状态管理 | Zustand | 轻量、简洁，比 Redux 更适合桌面应用 |
| 样式方案 | Tailwind CSS | 原子化CSS，快速开发 |
| 后端逻辑 | Electron Main Process (Node.js) | IMAP/SMTP、文件IO、系统集成 |
| IMAP/SMTP | `nodemailer` + `imap` | Node.js 生态最成熟的邮件库 |
| 数据库 | `better-sqlite3` + FTS5 | 高性能 SQLite 绑定 + 全文搜索 |
| 图表 | Recharts | React 原生，声明式API |
| 构建 | electron-builder | 一键打包 NSIS 安装程序 |
| i18n | react-i18next | 国际化支持 |

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    Renderer Process (React)             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ Account  │  │  Mail    │  │  Rule    │  │  Stats   │ │
│  │ Manager  │  │  List    │  │  Engine  │  │  Dashbd  │ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘ │
│       │            │            │            │         │
│       └────────────┴────────────┴────────────┘         │
│                       │ IPC                            │
├───────────────────────┼─────────────────────────────────┤
│                   Main Process                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │  IMAP    │  │  SMTP    │  │ SQLite   │  │ Notif.   │ │
│  │  Client  │  │  Client  │  │ + FTS5   │  │ Service  │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│       │            │            │            │         │
│  ═════╧════════════╧════════════╧════════════╧═════    │
│              IMAP/SMTP Servers (Internet)               │
└─────────────────────────────────────────────────────────┘
```

---

*文档生成完毕。以上推荐基于技术可行性、用户体验、开发成本三个维度的综合权衡。如果你对某一模块的推荐有不同看法，可以随时调整。*
